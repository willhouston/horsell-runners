---
import BaseLayout from "../layouts/BaseLayout.astro";
import PageHeader from "../components/PageHeader.astro";

const title = "Contact Us | Horsell Runners";
const description =
  "Get in touch with Horsell Runners. Ask a question, say hello, or enquire about joining.";

const success = Astro.url.searchParams.get("success") === "1";
---
<BaseLayout title={title} description={description}>
  <PageHeader
    title="Contact us"
    subtitle="Questions about pace groups, routes, or joining? Send us a message and we will reply soon."
  />

  <section class="section">
    <div class="container grid grid-2">
      <div class="card">
        <h3>Say hello</h3>
        <p>
          We aim to reply within a couple of days. If you need an immediate answer,
          join us at a run and chat in person.
        </p>
        <p class="meta">Meeting point: Outside the Beijing Restaurant, Horsell, Surrey.</p>
        <div class="btn-row">
          <a class="btn btn-outline" href="/runs">View Runs</a>
          <a class="btn btn-primary" href="/mailing-list">Join Mailing List</a>
        </div>
      </div>
      <div class="card">
        {success && (
          <p class="notice">Thanks for your message. We will be in touch soon.</p>
        )}
        <form
          class="form"
          name="contact"
          method="POST"
          action="/contact?success=1"
          data-netlify="true"
          netlify-honeypot="bot-field"
        >
          <input type="hidden" name="form-name" value="contact" />
          <p hidden>
            <label>
              Don’t fill this out: <input name="bot-field" />
            </label>
          </p>
          <label>
            Name
            <input class="input" type="text" name="name" required placeholder="Your name" />
          </label>
          <label>
            Email
            <input class="input" type="email" name="email" required placeholder="you@example.com" />
          </label>
          <label>
            Subject
            <input class="input" type="text" name="subject" required placeholder="How can we help?" />
          </label>
          <label>
            Message
            <textarea name="message" required placeholder="Tell us a little more..."></textarea>
          </label>
          <label class="checkbox-row">
            <input type="checkbox" name="consent" required />
            I consent to Horsell Runners contacting me about my enquiry.
          </label>
          <button class="btn btn-primary" type="submit">Send Message</button>
        </form>
        <p class="meta">
          This form is ready for Netlify. Swap it for your preferred provider when
          needed.
        </p>
      </div>
    </div>
  </section>
</BaseLayout>
